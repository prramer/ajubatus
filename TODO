To-Do


* Add the ability to recursively name photos separated into folders by capture
  date using the name of the folders as the capture date.

* Expand the file renaming capabilities to include renaming based on embedded metadata.

* Expand the file renaming to a configurable scheme that can be stored in a configuration file.
